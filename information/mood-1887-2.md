刚给服务器迁移完，把 rocket.chat 迁移到国外去了
原本就是一个 docker compose up + mongo 数据库迁移的事情
但是呢，由于同时更换了域名，导致 rocket.chat 的 cloud register 不认我的新服务器
只能在 cloud register 把之前的 licence 删除了之后 并在 mongo 里面删除 licence 才能成功
对了，docker 的 dns 也要手动指定一下，否则 self host 的 rocket.chat 连不上 cloud register